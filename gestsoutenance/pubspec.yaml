name: gestion_soutenances
description: Application de gestion des soutenances académiques
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  uuid: ^4.4.0
  http: ^1.1.0
  cupertino_icons: ^1.0.2  # Icônes iOS
  # material_design_icons_flutter: ^7.0.7296  # Décommentez si nécessaire
  # font_awesome_flutter: ^10.6.0  # Décommentez si nécessaire

flutter:
  # Utilisez la génération de matériel 3 (icons.useMaterial3 = true)
  # Cette ligne indique à Flutter d'utiliser Material 3 par défaut
  uses-material-design: true
  
  # Si vous voulez utiliser une police personnalisée, ajoutez ceci :
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700

  # Assets (si vous avez des images)
  assets:
    - assets/images/
    # - assets/icons/

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1